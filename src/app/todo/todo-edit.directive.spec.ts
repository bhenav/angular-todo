import { TodoEditDirective } from './todo-edit.directive';

describe('TodoEditDirective', () => {
  it('should create an instance', () => {
    const directive = new TodoEditDirective();
    expect(directive).toBeTruthy();
  });
});
