import { TodoDeleteDirective } from './todo-delete.directive';

describe('TodoDeleteDirective', () => {
  it('should create an instance', () => {
    const directive = new TodoDeleteDirective();
    expect(directive).toBeTruthy();
  });
});
